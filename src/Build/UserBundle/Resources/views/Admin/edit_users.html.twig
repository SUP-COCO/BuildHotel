{% extends '::template.html.twig' %}
{% block title %}{{ parent() }} Compte{% endblock %}
  
{% block body %}
	<div id="wrapper">
		{# {{form(form)}} #}
		{# <form action="{{path('admin_users_edit_check')}}" method="POST">
			Nom : <input type="text" id="username" value="{{ user.username }}" name="username" readonly><br>
			Adresse mail : <input type="text" id="mail" value="{{ user.mail }}" name="mail" readonly><br>
			Mission : <input type="text" id="motto" value="{{ user.motto }}" name="motto"><br>
			Crédits : <input type="text" id="credits" value="{{ user.credits }}" name="credits"><br>
			VIP : <input type="text" id="vip" value="{{ user.vip }}" name="vip"><br>
			Jetons : <input type="text" id="jetons" value="{{ user.jetons }}" name="jetons"><br>
			<input type="submit" value="Enregistrer">
		</form> #}
		{% if session is defined %}
			{# {% for session in session %}
				{{ session.name }}
			{% endfor %} #}
			<div id="update">{{session}}</div>
		{% endif %}
		{{user.username}}
		{{ form_start(form) }}
			{{ form_label(form.motto) }}
			{{ form_widget(form.motto) }}
			<br>
			{{ form_label(form.jetons) }}
			{{ form_widget(form.jetons) }}
			<br>
			{{ form_label(form.credits) }}
			{{ form_widget(form.credits) }}
			<br>
			{{ form_label(form.activityPoints) }}
			{{ form_widget(form.activityPoints) }}
			<br>
			{{ form_label(form.gender) }}
			{{ form_widget(form.gender) }}
			<br>
			{{ form_label(form.mail) }}
			{{ form_widget(form.mail) }}
			<br>

			{{ form_label(form.Modifier) }}
			{{ form_widget(form.Modifier) }}
		{{ form_end(form) }}
	</div>
{% endblock %}